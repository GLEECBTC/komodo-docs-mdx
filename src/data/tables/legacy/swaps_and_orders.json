{
  "ActiveSwapsRequest": {
    "data": [
      {
        "parameter": "include_status",
        "type": "bool",
        "required": false,
        "description": "Optional, defaults to false. Whether to include swap statuses in response"
      }
    ]
  },
  "ActiveSwapsResponse": {
    "data": [
      {
        "parameter": "uuids",
        "type": "array of strings",
        "required": true,
        "description": "A list of currently active swap UUIDs"
      },
      {
        "parameter": "statuses",
        "type": "object (map)",
        "required": false,
        "description": "Only visible if include_status=true. Map of SwapStatus objects by UUID (see /komodo-defi-framework/api/common_structures/swaps/#swap-status)"
      }
    ]
  },
  "AllSwapsUuidsByFilterRequest": {
    "data": [
      {
        "parameter": "my_coin",
        "type": "string",
        "required": false,
        "description": "Return only swaps that match the swap.my_coin = request.my_coin condition"
      },
      {
        "parameter": "other_coin",
        "type": "string",
        "required": false,
        "description": "Return only swaps that match the swap.other_coin = request.other_coin condition"
      },
      {
        "parameter": "from_timestamp",
        "type": "number (timestamp in seconds)",
        "required": false,
        "description": "Return only swaps that match the swap.started_at >= request.from_timestamp condition"
      },
      {
        "parameter": "to_timestamp",
        "type": "number (timestamp in seconds)",
        "required": false,
        "description": "Return only swaps that match the swap.started_at < request.to_timestamp condition"
      }
    ]
  },
  "AllSwapsUuidsByFilterResponse": {
    "data": [
      {
        "parameter": "uuids",
        "type": "array of strings",
        "required": true,
        "description": "UUIDs of swaps that match the selected filters"
      },
      {
        "parameter": "my_coin",
        "type": "string",
        "required": false,
        "description": "my_coin that was set in the request; null if not provided"
      },
      {
        "parameter": "other_coin",
        "type": "string",
        "required": false,
        "description": "other_coin that was set in the request; null if not provided"
      },
      {
        "parameter": "from_timestamp",
        "type": "number (timestamp in seconds)",
        "required": false,
        "description": "from_timestamp that was set in the request; null if not provided"
      },
      {
        "parameter": "to_timestamp",
        "type": "number (timestamp in seconds)",
        "required": false,
        "description": "to_timestamp that was set in the request; null if not provided"
      },
      {
        "parameter": "found_records",
        "type": "number",
        "required": true,
        "description": "Number of UUIDs that match the selected filters"
      }
    ]
  },
  "BestOrdersRequest": {
    "data": [
      {
        "parameter": "coin",
        "type": "string",
        "required": true,
        "description": "Ticker of the coin to price orders for"
      },
      {
        "parameter": "action",
        "type": "string",
        "required": true,
        "description": "Use \"buy\" to request liquidity to acquire the coin, or \"sell\" to offer the coin"
      },
      {
        "parameter": "volume",
        "type": "string (decimal)",
        "required": true,
        "description": "Amount of `coin` that must be filled by the returned orders"
      },
      {
        "parameter": "exclude_mine",
        "type": "bool",
        "required": false,
        "description": "Optional. Defaults to false. If true, exclude the node's own orders from the response"
      }
    ]
  },
  "BestOrdersResponse": {
    "data": [
      {
        "parameter": "result",
        "type": "object (map)",
        "required": true,
        "description": "Map of `ticker` -> array of standard OrderDataV1 objects representing the best priced counterparties (see /komodo-defi-framework/api/common_structures/orders/#order-data-v1)"
      },
      {
        "parameter": "result.original_tickers",
        "type": "object (map)",
        "required": false,
        "description": "Present when `orderbook_ticker` remaps a coin. Each key is the canonical ticker and the value is a list of original tickers that appear in the response"
      }
    ]
  },
  "BuyRequest": {
    "data": [
      {
        "parameter": "base",
        "type": "string",
        "required": true,
        "description": "Ticker of the coin to receive (the coin you are buying)"
      },
      {
        "parameter": "rel",
        "type": "string",
        "required": true,
        "description": "Ticker of the coin you are spending"
      },
      {
        "parameter": "price",
        "type": "number | RationalValue | FractionalValue",
        "required": true,
        "description": "Price in `rel` per one unit of `base` (supports decimal numbers, num-rational arrays, or fraction objects)"
      },
      {
        "parameter": "volume",
        "type": "number | RationalValue | FractionalValue",
        "required": true,
        "description": "Amount of `base` to purchase; must satisfy each coin's `min_trading_vol` limits"
      },
      {
        "parameter": "min_volume",
        "type": "number | RationalValue | FractionalValue",
        "required": false,
        "description": "Minimum amount of `base` that must remain if the order converts to maker mode"
      },
      {
        "parameter": "match_by",
        "type": "MatchBy object",
        "required": false,
        "description": "Restrict matches to specific pubkeys or order UUIDs (ignored after conversion to maker)"
      },
      {
        "parameter": "order_type",
        "type": "OrderType object",
        "required": false,
        "description": "Controls maker/taker behaviour (`GoodTillCancelled`, `FillOrKill`, etc.)"
      },
      {
        "parameter": "base_confs / rel_confs",
        "type": "number",
        "required": false,
        "description": "Override the confirmation requirements for base or rel sides"
      },
      {
        "parameter": "base_nota / rel_nota",
        "type": "bool",
        "required": false,
        "description": "Override whether notarization is required for base or rel sides"
      },
      {
        "parameter": "save_in_history",
        "type": "bool",
        "required": false,
        "description": "Defaults to true. Set false to skip persisting the short order history locally"
      }
    ]
  },
  "BuyResponse": {
    "data": [
      {
        "parameter": "action",
        "type": "string",
        "required": true,
        "description": "Always \"Buy\" for this method"
      },
      {
        "parameter": "base / rel",
        "type": "string",
        "required": true,
        "description": "Tickers of the trade pair"
      },
      {
        "parameter": "base_amount / rel_amount",
        "type": "string",
        "required": true,
        "description": "Decimal string totals representing the matched trade size"
      },
      {
        "parameter": "base_amount_rat / rel_amount_rat",
        "type": "RationalValue",
        "required": false,
        "description": "Rational representation of the matched trade size"
      },
      {
        "parameter": "uuid",
        "type": "string",
        "required": true,
        "description": "UUID of the buy request"
      },
      {
        "parameter": "conf_settings",
        "type": "ConfSettings object",
        "required": false,
        "description": "Effective confirmation and notarization settings applied to this order"
      },
      {
        "parameter": "match_by / order_type",
        "type": "object",
        "required": false,
        "description": "Echoes the match filters and order type used for this order"
      },
      {
        "parameter": "base_orderbook_ticker / rel_orderbook_ticker",
        "type": "string | null",
        "required": false,
        "description": "Present when coins are remapped via `orderbook_ticker`"
      }
    ]
  },
  "SetpriceRequest": {
    "data": [
      {
        "parameter": "base",
        "type": "string",
        "required": true,
        "description": "Ticker of the coin you want to sell (maker side)"
      },
      {
        "parameter": "rel",
        "type": "string",
        "required": true,
        "description": "Ticker of the coin you want to receive"
      },
      {
        "parameter": "price",
        "type": "number | RationalValue | FractionalValue",
        "required": true,
        "description": "Desired amount of `rel` per 1 unit of `base` (supports decimal strings, num-rational arrays, or fraction objects)"
      },
      {
        "parameter": "volume",
        "type": "number | RationalValue | FractionalValue",
        "required": false,
        "description": "Maximum amount of `base` you are willing to sell; required when `max` is false. Must satisfy each coin's `min_trading_vol` limits for both the base amount and the implied rel amount (`volume * price`)"
      },
      {
        "parameter": "min_volume",
        "type": "number | RationalValue | FractionalValue",
        "required": false,
        "description": "Minimum `base` volume that must remain for the order to stay open; must be ≤ `volume` and satisfy `min_trading_vol` limits for both coins"
      },
      {
        "parameter": "max",
        "type": "bool",
        "required": false,
        "description": "If true, uses the entire available `base` balance (leaving 0.001 `base` in reserve for fees)"
      },
      {
        "parameter": "cancel_previous",
        "type": "bool",
        "required": false,
        "description": "Defaults to true. Cancels any existing maker orders for the same pair before placing the new order"
      },
      {
        "parameter": "base_confs / rel_confs",
        "type": "number",
        "required": false,
        "description": "Override the required blockchain confirmations for the base or rel side"
      },
      {
        "parameter": "base_nota / rel_nota",
        "type": "bool",
        "required": false,
        "description": "Override whether dPoW notarization is required for the base or rel side"
      },
      {
        "parameter": "save_in_history",
        "type": "bool",
        "required": false,
        "description": "Defaults to true. Set to false to skip persisting the short order history locally (the order status is still tracked while active)"
      },
      {
        "parameter": "timeout_in_minutes",
        "type": "number (integer)",
        "required": false,
        "description": "Optional expiration timer for maker orders. When set, Komodo DeFi Framework automatically cancels the order after the specified number of minutes"
      }
    ]
  },
  "SetpriceResponse": {
    "data": [
      {
        "parameter": "result",
        "type": "object",
        "required": true,
        "description": "Order object describing the maker order that was created"
      },
      {
        "parameter": "result.base / result.rel",
        "type": "string",
        "required": true,
        "description": "Tickers of the order's trade pair"
      },
      {
        "parameter": "result.price / result.price_rat",
        "type": "string | RationalValue",
        "required": true,
        "description": "Maker price in decimal and rational representations"
      },
      {
        "parameter": "result.max_base_vol / result.max_base_vol_rat",
        "type": "string | RationalValue",
        "required": true,
        "description": "Maximum base volume that can be filled from this order"
      },
      {
        "parameter": "result.min_base_vol / result.min_base_vol_rat",
        "type": "string | RationalValue",
        "required": false,
        "description": "Minimum base volume the order accepts"
      },
      {
        "parameter": "result.created_at / result.updated_at",
        "type": "number (timestamp in ms)",
        "required": true,
        "description": "Creation and last update timestamps for the order"
      },
      {
        "parameter": "result.matches",
        "type": "object (map)",
        "required": true,
        "description": "Map of currently matched swaps keyed by counterparty UUID; empty for newly created orders"
      },
      {
        "parameter": "result.started_swaps",
        "type": "array of strings",
        "required": true,
        "description": "UUIDs of swaps that were initiated from this order"
      },
      {
        "parameter": "result.uuid",
        "type": "string",
        "required": true,
        "description": "UUID of the created maker order"
      },
      {
        "parameter": "result.conf_settings",
        "type": "ConfSettings object",
        "required": false,
        "description": "Effective confirmation/notarization settings applied to the order"
      },
      {
        "parameter": "base_orderbook_ticker / rel_orderbook_ticker",
        "type": "string | null",
        "required": false,
        "description": "Present when coins are remapped via `orderbook_ticker`; null otherwise"
      }
    ]
  }
}


