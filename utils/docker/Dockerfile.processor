FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir requests

# Copy the response processing script
COPY utils/scripts/process_responses.py /app/process_responses.py

CMD ["python", "/app/process_responses.py"]
